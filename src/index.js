import "../packages/use-countdown/src/UseCountdown.bs"
import "../packages/use-previous/src/UsePrevious.bs"

import React from 'react';

export default () => {
  <div className="App">
    <header className="App-header">
      <p>
        Page has been open for <code>{count}</code> seconds.
        </p>
    </header>
  </div>
}